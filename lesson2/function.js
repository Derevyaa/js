function isAdult(age) {
    if (age >= 18) {
      return true;
    } else {
      return false;
    }
  }
  
  let result = isAdult("25");
  console.log(result);

  function isAdult2(age) {
    if (age >= 18) {
      return true;
    } else {
      return false;
    }
  }

  let result2 = isAdult2(25);